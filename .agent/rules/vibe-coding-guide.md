---
trigger: always_on
---

⚡ ELITE AUTOMATED WORKFLOW SYSTEM
🔍 @Audit — DEEP ARCHITECTURE SCAN
Execution:

COMPREHENSIVE ANALYSIS

Detect DRY violations with line-level precision
Map prop drilling chains across component trees
Identify unnecessary re-renders + missing memoization
Flag unused dependencies, dead code, circular imports
Analyze bundle size impact + optimization opportunities

DELIVERABLES

Markdown report with severity ratings (Critical/High/Medium/Low)
Before/After code comparisons
Automated refactored code with architectural improvements
Performance impact estimates (render time reduction, bundle savings)

🚀 @Feature — PRODUCTION-GRADE CONSTRUCTION
Iron-Clad Sequence:

ARCHITECTURE BLUEPRINT: Define TS interfaces, data flow, dependencies
ATOMIC COMPONENTS: Build primitive, reusable UI (100% decoupled)
LOGIC LAYER: Extract to custom hooks with proper error handling
STATE MANAGEMENT: Integrate loading/error/success states + optimistic updates
RESILIENCE: Add Error Boundaries, retry logic, fallback UI, accessibility
QUALITY: Unit tests (hooks), integration tests (components), E2E scenarios

Output: Fully integrated, production-ready feature with tests + docs.

🐛 @BugFix — ROOT CAUSE ELIMINATION
NO PATCHES. ARCHITECTURAL SURGERY ONLY.
Protocol:

ROOT CAUSE ANALYSIS: Trace full execution path, identify architectural weaknesses
SURGICAL FIX: Fix at origin, refactor if architecture is flawed
PREVENTION LAYER: Add try-catch, Error Boundaries, type guards, validation, logging
REGRESSION PREVENTION: Write failing test → Fix bug → Verify → Add edge cases

Output: Fixed code + error boundaries + test suite preventing recurrence.

🔐 @SecurityCheck — FORTRESS-LEVEL AUDIT
ZERO-TRUST SCAN:

CREDENTIAL EXPOSURE

Scan for hardcoded API keys, tokens, passwords
Check git history for sensitive data
Verify environment variable usage

INPUT VALIDATION

Identify all user input entry points
Implement Zod schemas for every API payload
Add XSS sanitization (DOMPurify)
Validate file uploads (type, size, content)

API SECURITY

Verify CORS (no wildcard origins)
Check CSRF tokens, JWT expiration, rate limiting
Audit authentication flows

DEPENDENCY SCAN

Run npm audit for critical vulnerabilities
Flag outdated packages with CVEs

DATA PROTECTION

Identify sensitive data in localStorage/sessionStorage
Verify encryption for transmission
Sanitize error messages (no stack traces to users)

Output: Security report + implemented fixes + security score.

🎯 GLOBAL EXECUTION STANDARDS
Every workflow MUST:

✅ Production-ready code (zero TODOs/placeholders)
✅ Comprehensive error handling
✅ SOLID principles + TypeScript strict mode
✅ Inline documentation for complex logic
✅ Performance optimization (memoization, lazy loading)
✅ Accessibility (WCAG 2.1 AA)
✅ Actionable reports with metrics

Quality Gates:

Zero ESLint errors
100% TypeScript (no any)
Functions < 50 lines
Cyclomatic complexity < 10
Test coverage > 80%
